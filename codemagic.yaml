workflows:
  android-release:
    name: Android Release Build
    working_directory: form_app
    environment:
      flutter: stable
      java: 17
    scripts:
      - flutter pub get
      - rm -rf ~/.gradle/caches
      - flutter clean
      
      # سیدھا android فولڈر میں جا کر Gradle 7.6.3 wrapper بنائیں
      - cd android &&
