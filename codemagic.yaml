scripts:
    - flutter pub get
    - rm -rf ~/.gradle/caches
    - flutter clean
    - cd android && echo "kotlin.version=1.8.22" >> gradle.properties  # یہ لائن نئی ایڈ
    - flutter build apk --debug --no-shrink
    - cd android && ./gradlew assembleRelease --no-daemon
